services:
  roster:
    image: ghcr.io/stockyard-dev/stockyard-roster:latest
    ports: ["8930:8930"]
    environment: [PORT=8930, DATA_DIR=/data]
    volumes: [roster-data:/data]
    restart: unless-stopped
volumes:
  roster-data:
